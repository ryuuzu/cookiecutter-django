volumes:
  production_traefik: {}
  {% if cookiecutter.use_celery == 'y' %}
  production_redis_data: {}
  {% endif %}


services:
  django:{% if cookiecutter.use_celery == 'y' %} &django{% endif %}
    build:
      context: .
      dockerfile: ./compose/production/django/Dockerfile

    image: {{ cookiecutter.project_slug }}_production_django
    depends_on:
      - redis
    env_file:
      - ./.envs/.production/.django
    command: /start
  # Postgres is expected to be provided by your external DB server in production.
  # The local containerized Postgres service has been removed from this template.

  traefik:
    build:
      context: .
      dockerfile: ./compose/production/traefik/Dockerfile
    image: {{ cookiecutter.project_slug }}_production_traefik
    depends_on:
      - django
    volumes:
      - production_traefik:/etc/traefik/acme
    ports:
      - '0.0.0.0:80:80'
      - '0.0.0.0:443:443'
      {%- if cookiecutter.use_celery == 'y' %}
      - '0.0.0.0:5555:5555'
      {%- endif %}

  redis:
    image: docker.io/redis:7.2
    {% if cookiecutter.use_celery == 'y' %}
    volumes:
      - production_redis_data:/data
    {% endif %}


  {%- if cookiecutter.use_celery == 'y' %}

  celeryworker:
    <<: *django
    image: {{ cookiecutter.project_slug }}_production_celeryworker
    command: /start-celeryworker

  celerybeat:
    <<: *django
    image: {{ cookiecutter.project_slug }}_production_celerybeat
    command: /start-celerybeat

  flower:
    <<: *django
    image: {{ cookiecutter.project_slug }}_production_flower
    command: /start-flower
  {%- endif %}
  {%- if cookiecutter.cloud_provider == 'AWS' %}

  awscli:
    build:
      context: .
      dockerfile: ./compose/production/aws/Dockerfile
    env_file:
      - ./.envs/.production/.django
    # backups volume removed; adjust your backup strategy to use external storage
  {%- endif %}
